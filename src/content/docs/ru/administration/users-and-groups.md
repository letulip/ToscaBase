---
title: Пользователи и группы
description: Создавайте пользователей и группы в многопользовательском workspace, задавайте и меняйте пароли, выдавайте роль Admins, ограничивайте разделы через группы owning и viewing, отключайте пользователей и читайте personal data report.
level: 4
sidebar:
  order: 20
sources:
  - id: qeYPDWYYEX4
    title: "Tricentis Tosca Tutorial Part-14: Tosca User Management, Tosca User Group, Tosca Password Reset"
    url: https://www.youtube.com/watch?v=qeYPDWYYEX4
    at: "01:21"
  - id: PDP4hgWD9zY
    title: "Tosca Tutorial | Lesson 91 - Create and Manage User Groups with Users | Multi-User Workspace |"
    url: https://www.youtube.com/watch?v=PDP4hgWD9zY
    at: "00:14"
---

Управление пользователями (user management) существует только в [многопользовательском workspace](/ToscaBase/ru/administration/multi-user-workspaces/). Оно отвечает на два вопроса: *аутентификация* (кто может войти и с каким паролем) и *авторизация* (какая группа может менять или просматривать какую часть workspace). И то и другое настраивает администратор внутри Tosca Commander; отдельного инструмента администрирования нет.

## Где находится управление пользователями

Не в верхнем меню. Выберите корневой узел проекта и откройте его вкладку **User management**; в правой части окна перечислены группы и пользователи. Любое изменение здесь требует, чтобы корень проекта был взят на check-out (взятие на редактирование), и любое изменение нужно сдать (**Check In All**), прежде чем его увидят другие пользователи, — включая новый пароль.

По умолчанию есть две группы:

- `Admins` — администраторы. Пользователь по умолчанию `Admin` состоит в ней, и его нельзя отключить.
- `All users` — здесь появляется каждый созданный вами пользователь.

## Создание группы

Щёлкните правой кнопкой корень проекта (или верх области управления пользователями) и выберите **Create user group**, затем переименуйте группу. Группы могут отражать роли (в источнике создаются `Tosca specialists` и `Tosca design specialists`) или проекты, которым нужен отдельный доступ. Удаление группы не удаляет её пользователей.

## Создание пользователя

Щёлкните группу правой кнопкой и выберите **Create user** (у группы `All users` та же команда). Пользователь создаётся с именем-заготовкой вроде `User 6`; переименуйте его. Свойства пользователя:

| Свойство | Значение |
|---|---|
| **Enabled** | Включено по умолчанию. Снимите флажок, чтобы закрыть пользователю вход (см. ниже). |
| **Description** | Произвольный текст, например назначение пользователя или группы. |
| **Domain** | Необязательный домен пользователя. |
| **Set result allowed** | Если снято, пользователь не может вручную выставить результат выполнения в passed или failed; показывается только результат, полученный Tosca. |
| **Level** | `Basic`, `Advanced` или `Expert`. |
| **Owning group** | `Admins` или `All users`: только члены владеющей группы могут менять эту запись пользователя или группы. |

:::note
Источники расходятся насчёт **Level**: Part 14 называет его уровнем квалификации пользователя, урок 91 говорит, что он даёт разный доступ к разным разделам. Ни один не показывает конкретного эффекта. Считайте это классификацией, пока не проверите в своей версии.
:::

Пользователь может состоять в нескольких группах. Перетащите пользователя в другую группу, чтобы добавить его туда; записи — экземпляры одного пользователя, и изменение в одном месте (пароль, флажок Enabled) отражается во всех. Чтобы сделать пользователя администратором, перетащите его в `Admins`.

## Пароли

У нового пользователя пароль пустой, поэтому сразу задайте его: щёлкните пользователя правой кнопкой, выберите **Set password**, введите и подтвердите. Эту команду видит только администратор; с её помощью он может в любой момент задать или сбросить пароль любого пользователя.

Вошедший пользователь меняет собственный пароль, щёлкнув правой кнопкой корень проекта (взятый на check-out) и выбрав **Change my password**: ввести старый пароль, затем дважды новый. Сделайте check-in; новый пароль действует со следующего входа.

Чтобы проверить новый вход, сделайте check-in, закройте workspace и откройте заново (существующему workspace на том же репозитории сначала нужен **Update All**, либо создайте новый workspace на общем репозитории). Окно входа появляется не сразу; имена и пароли чувствительны к регистру.

## Owning group и viewing group: права доступа

Доступ выдаётся на раздел или папку через два свойства, видимые после check-out объекта в его свойствах:

- **Owning group name** — группа, которой разрешено менять объект (создавать, удалять, обновлять). По умолчанию `All users`, наследуется от родителя.
- **Viewing group name** — по умолчанию пусто. Если задано, члены этой группы могут открывать объект, но не менять.

Пример из источника: на разделе `Modules` установить owning group `Tosca design specialists`, а viewing group — `Tosca specialists` (или оставить `All users`); специалисты по дизайну ведут Module, все остальные могут только читать. То же работает на любой папке компонентов, так что команде проекта можно выдать ровно её папку.

## Отключение пользователя

Выберите пользователя и снимите флажок **Enabled**; изменение реплицируется во все экземпляры пользователя. После **Check In All** пользователь больше не может войти. Отключённых пользователей можно скрыть или показать переключателем **Show disabled users** в параметрах **View** управления пользователями.

## Personal data report

Tosca может сформировать **personal data report** по каждому пользователю — Excel-файл, создаваемый внутри workspace, чтобы проследить, что пользователь делал: ID машины, ID workspace, ID и имя пользователя, ревизии с датой создания, ID изменённых объектов и комментарии. У пользователя, который ни разу не входил, отчёт пуст.

## См. также

- [Многопользовательские workspace](/ToscaBase/ru/administration/multi-user-workspaces/): check-out и доступный только администратору **Revoke checkout**.
- [Процесс ревью](/ToscaBase/ru/best-practices/review-process/): папочный workflow, который можно закрепить через группы owning и viewing.
