---
title: Повторения
description: Выполнение TestStep папки фиксированное число раз через свойство Repetition, задаваемое через выбор колонок или свойства папки.
level: 1
sidebar:
  order: 40
sources:
  - id: jWpcEtvOo-w
    title: "Tosca Tutorial | Lesson 48 - Run Test Steps Repeatedly using Repetitions |"
    url: https://www.youtube.com/watch?v=jWpcEtvOo-w
    at: "02:06"
---

**Repetition** (повторение) — число на папке TestStep, заставляющее Tosca выполнить каждый TestStep внутри этой папки столько раз подряд. Это простейший способ повторить шаги: без условия, без буфера-счётчика, без объекта цикла. Типичные применения — сценарий, в котором действие действительно повторяется (добавить один товар в корзину пять раз), или грубая проверка нагрузки на один элемент. Там, где достаточно фиксированного счёта, Tricentis предпочитает это циклу `While`; см. [Структура TestCase](/ToscaBase/ru/best-practices/test-case-structure/).

## Где можно задать Repetition

Только на **папке** внутри TestCase. Свойство недоступно на отдельном TestStep и на самом TestCase. Чтобы повторить один шаг, положите его в отдельную папку.

Пример TestCase состоит из трёх частей: шаги логина, папка `Add products` с одним TestStep, кликающим `Add to cart`, и шаг закрытия приложения. Repetition ставится на `Add products`.

## Способ 1: колонка Repetition

1. В секции TestCases щёлкните правой кнопкой по строке заголовков колонок и выберите **Column Chooser**.
2. Найдите колонку **Repetition** и перетащите её в строку заголовков или дважды щёлкните по ней. Колонка появится для всех TestCase.
3. В строке папки введите число повторений, например `5`.

Чтобы убрать колонку, перетащите её заголовок из строки заголовков, пока не появится крестик, и отпустите.

## Способ 2: свойства папки

1. Выделите папку и откройте панель **Properties**.
2. Задайте свойство **Repetition**, например `4`.

Оба способа задают одно и то же свойство; изменение в панели свойств обновляет колонку и наоборот.

## Что вы увидите

- **Значок папки.** Папка с Repetition показывает закруглённую стрелку назад. Уберите Repetition — значок вернётся к обычной папке. Это самый быстрый способ заметить повторяемые шаги в большом TestCase.
- **Результаты.** В результате ScratchBook или ExecutionList узел папки перечисляет повторения; при раскрытии видно шаг, выполненный по разу на каждое повторение (пять записей для `5`, четыре для `4`).

## См. также

- [Управление потоком](/ToscaBase/ru/test-cases/control-flow/) — циклы `While` и `Do`, когда счёт зависит от условия.
- [Повторения выполнения и бизнес-TestCase](/ToscaBase/ru/execution/execution-repetitions-and-business-test-cases/) — повторение целого TestCase из ExecutionList, это другое свойство.
- [Экземпляры и комбинаторика](/ToscaBase/ru/test-case-design/instances-and-combinatorics/) — выполнение одних шагов с разными данными; для этого предназначен TestCase design, а не Repetition.
